# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Build

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: ["windows-latest"]
        config: ["Debug", "Release"]
        platform: ["ARM64", "Win32", "x64"]
        solution: ["UIforETW/UIforETW.sln"]
        include:
          - platform: "AnyCPU"
            solution: "TraceProcessors/TraceProcessors.sln"
            config: "Debug"
          - platform: "AnyCPU"
            solution: "TraceProcessors/TraceProcessors.sln"
            config: "Release"

    runs-on: windows-latest

    # ⚙️ Build • Win32 • Debug
    name: "⚙️ Build • ${{ matrix.platform }} • ${{ matrix.config }} ⚙️"

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Setup MSBuild 🛠️
        uses: microsoft/setup-msbuild@v2

      - name: NuGet Restore 📩
        working-directory: ${{ github.workspace }}
        run: nuget restore "${{ matrix.solution }}"

      - name: Build ⚙️
        working-directory: ${{ github.workspace }}
        run: msbuild /m /p:Platform="${{ matrix.platform }}" /p:Configuration="${{ matrix.config }}" "${{ matrix.solution }}"
